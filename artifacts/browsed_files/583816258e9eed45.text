/** Platform-neutral vocabulary shared by MLAI's public surfaces. */
export type ProductAccent = "wdbx" | "abi" | "abbey";

/** Public figures must retain one of these evidence classifications. */
export type Provenance = "measured" | "target" | "reported";

/** Keep persona labels separate from the product accent axis. */
export type Persona = "abbey" | "aviva" | "abi";
