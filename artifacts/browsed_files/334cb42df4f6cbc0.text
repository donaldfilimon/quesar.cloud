# Brand — ABI

**Umbrella:** Intelligence Without Limits.

**ABI:** Local AI orchestration with inspectable memory.

**Twin:** abi = runtime/WDBX; abbey = companion that will not claim what the ledger cannot prove.

## Policy gating

IWL is not unlimited/AGI. Do not present synthetic benchmarks as measured, multi-host HA as Current, certifications, NYX/Studio/public API as Current, browser autonomy, or required cloud backends.

This site (`site/`) is an ABI surface and may carry IWL. Quesar (private AI ops) never does. One tagline per surface. Vision decks only: *One Intelligence Platform. Infinite Possibilities.*
