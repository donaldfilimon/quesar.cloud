node_modules/
.env
.env.*
.output/
dist/
# Build output (Nitro Vercel preset); Vercel regenerates it from source.
.vercel/
# Local QA scratch.
screenshots/
artifacts/
# Skill-sync mirror of .claude/skills (regenerated by /sync-clis; the .claude copy is canonical).
.agents/
